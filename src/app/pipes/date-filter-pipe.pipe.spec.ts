import { DateFilterPipePipe } from './date-filter-pipe.pipe';

describe('DateFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DateFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
