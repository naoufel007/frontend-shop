import { MonoAgencePipe } from './mono-agence.pipe';

describe('MonoAgencePipe', () => {
  it('create an instance', () => {
    const pipe = new MonoAgencePipe();
    expect(pipe).toBeTruthy();
  });
});
